# 2026-08-12

## やったこと
- メモリストアで並び替えアルゴリズムを実装した
- 並び替えアルゴリズム
  - 対象のIDを削除する
  - previousIDがnullなら先頭に追加する
  - previousIDがnullではないなら、その位置を探して後に追加する
  - 全部itemsを取り出してpositionを振り直す

## 分かったこと
- Go の書き方をすごく忘れている。そもそも構文を思い出したり、どうやってそれ実装するんだっけ？を調べる時間がかかる。AI縛りだと。
- 先に対象を消してから挿入する。後に消すのはどちらを消せばいいかわからなくなる。そのため先に対象を消すが、その対象のデータは保持しておく必要がある。
- 実装したアルゴリズムだとpositionを振り直すコストが大きいな

## 分からなかったこと
- DB では挿入位置とかないけどどうするんだ？全部取得してきて、そこから並び替えして、Upsert とか？

## 次に試したいこと
-
